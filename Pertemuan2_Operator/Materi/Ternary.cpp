#include <iostream>
using namespace std;

int main() {
    system("cls");
    // Ternary Operator (?:)
    // Kind of like if else tapi jika hanya untuk satu statement
    bool status = true;
    string hasil = (status ? "Benar" : "Salah");
    cout << hasil << endl;

    return 0;
}